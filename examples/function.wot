a := 4 + 5 * 12

foo :: fn(a: int, b: int, c: int) -> int {
    d := a + b
    if (d>0) {
        return 2 * a + b * b - 4 * a * c
    } else {
        return 0
    }
}

println(foo(a, 2 * a, 3 * a))
